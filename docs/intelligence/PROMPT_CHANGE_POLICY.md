# Prompt change policy

Prompt changes require a new version and content hash, a plain-language diff,
declared risk impact, complete hard-safety regression results, engineering and
safety review, and a tested rollback to the last approved prompt/version pair.

No prompt is editable through an admin interface. Phase A uses deterministic
execution and treats the prompt catalogue as governed source material.
