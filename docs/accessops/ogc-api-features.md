# OGC API Features

The OGC API Features projection is read-only and gated by `ACCESSOPS_OPEN_DATA_EXPORTS_ENABLED`, which defaults to false.

Exports must filter participant data and restricted geometry. Open-data freshness is not live operational status, and stale data is not current.
