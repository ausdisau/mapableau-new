# Operations runbook

Keep external feeds, outdoor providers, open-data exports, production webhooks, status subscriptions, sensor feeds, and indoor imports disabled unless explicitly approved.

Before any production operation, validate schema, review diffs, confirm feature flags, and verify that no participant journeys or restricted geometry appear in public outputs.
