# SensorThings

AccessOps supports a sensing-only SensorThings profile. Tasking is disabled, and incoming queries are validated for injection patterns.

SensorThings observations are evidence inputs. They do not actuate devices and do not automatically become live status.
