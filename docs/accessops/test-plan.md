# AccessOps test plan

Critical tests cover public identifiers, observations, status projection, twin graph constraints, unknown reliability, incident lifecycle, work-order verification, sensor no-actuation, protocol injection/SSRF checks, partner DTOs, webhooks, open-data privacy, deny-default policy, docs, config, AURA manifest, and keyboard authoring.

Tests must preserve Waves 2-11 safety boundaries.
