# Outdoor access graph

Sidewalk, crossing, kerb, surface and gradient models with per-segment provenance.

## Rules

- Missing properties remain unknown — never default to accessible.
- No production outdoor-routing provider is activated by default.
- OpenStreetMap-derived data retains attribution and licensing.
- Map matching does not reveal participant travel history.
- Routes are never described as guaranteed.
