# OGC API Features profile

Read-only public projection gated by `ACCESSOPS_OPEN_DATA_EXPORTS_ENABLED` (default false).

## Rules

- Explicit conformance classes; WGS 84 where required.
- Pagination and bbox validation; limit caps.
- No restricted features; no participant data; no write operations in the public profile.
