# Web of Things

WoT integration is read-only. Thing descriptions can expose safe property affordances, but action affordances remain disabled.

WoT forms must reject private-network targets and insecure production URLs to reduce SSRF and unsafe-control risk.
