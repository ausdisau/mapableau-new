# GTFS profile

Approved GTFS Schedule and GTFS Realtime accessibility projection.

## Rules

- Feed version and age retained.
- Missing wheelchair boarding data remains unknown.
- Accessibility alerts create candidate status events only.
- Vehicle positions remain advisory.
- No participant location is sent to feed providers.
- Feeds are disabled until explicitly approved (`ACCESSOPS_EXTERNAL_FEEDS_ENABLED=false` by default).
