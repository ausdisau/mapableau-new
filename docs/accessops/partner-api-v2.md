# Partner API v2

Partner v2 endpoints use API-key authentication, tenant-scoped queries, and explicit scopes for assets, features, status, incidents, reliability, observations, and webhooks.

Partner DTOs exclude ownership internals, reporter identity, participant data, and restricted geometry. Write scopes are separate from read scopes.
