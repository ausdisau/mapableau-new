# Scripts and audits

AccessOps scripts write JSON artifacts under `artifacts/accessops`. Dry runs must not fabricate owners, uptime, source records, or status.

Artifacts should contain aggregate counts, safe identifiers, and disclaimers only. Scripts do not deploy, activate feeds, or enable webhook delivery.
