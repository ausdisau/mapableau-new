# Access as Infrastructure — Prompts 02–10 Plan

## CURRENT STATE (prerequisite audit)

- Repo: MapableAU / Next.js / pnpm / Vitest / Playwright / Prisma
- Canonical Access stack exists: `lib/access/infrastructure`, GAIS (`lib/gais`), navigate (`lib/access/navigate`), experience quick observations
- **Prompt 01 (Open Infrastructure + Panoramax) is NOT present** — no Access Integration Gateway, no Panoramax adapter, no Coolify open-infra boundary
- Existing `lib/integrations` is generic connector registry (not evidence gateway)
- Storage: local disk helpers only; Terraform AWS object storage; no R2-coupled GAIS

## REUSABLE COMPONENTS

- Observation + provenance: `lib/access/infrastructure/{observation-service,provenance,flags}`
- Evidence provenance contracts: `packages/contracts/src/evidence-provenance.ts`
- Navigate constraints/objectives: `lib/access/navigate/*`
- Quick observation UX patterns: `lib/access/experience/*`
- Feature-flag fail-closed pattern throughout Access modules

## GAP → IMPLEMENTATION SEQUENCE

| Phase | Gap | Approach |
| ----- | --- | -------- |
| P01 repair | Integration gateway + Panoramax | `lib/integrations/access/*` + Coolify docs; flags OFF |
| P02 | Sidewalk + Quests | Adapter + domain quest engine + map-independent flow |
| P03 | Open311 civic bridge | Draft-first CivicAccessIssue + Open311 provider |
| P04 | Journey intelligence | Provider-neutral routing + MapAble overlay on navigate |
| P05 | ODK + Missions | Adapter + AccessMission domain + offline draft sync |
| P06 | SensorThings | Adapter + temporal evidence projection |
| P07 | Public interoperability | Public-safe feature projection API |
| P08 | Community Access Graph | Metrics/clustering without people scoring |
| P09 | Agentic Access | Bounded agents L0–L3 + tools + evals |
| P10 | Hardening | Threat model, flag matrix, readiness GO/NO-GO |

## DATA IMPACT

- Prefer additive domain contracts over Prisma migrations
- No destructive migrations
- External payloads never write Prisma directly

## SECURITY / PRIVACY / A11Y

- Fail-closed flags; no autonomous civic submit; no participant data in public APIs
- Quest/civic/route flows map-independent, keyboard-first, WCAG 2.2 AA target

## STOP CONDITIONS

Production credentials, Coolify deploy, destructive schema, licence conflict, legal policy decisions.

## FLAGS (all default OFF)

See `.env.example` additions under `MAPABLE_OPEN_INFRASTRUCTURE_*` and phase-specific flags.
