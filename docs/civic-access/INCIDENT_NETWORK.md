# Federated Access Incident Network

**Status:** Disabled — `MAPABLE_CIVIC_INCIDENT_NETWORK_ENABLED=false`

Reuse `IndoorAccessibilityIncident` and existing moderation. Future signed exchange envelopes must not auto-publish raw community reports and must never reveal participant journeys.

Wave 1 lift “operational status” claims remain **unknown** without a live feed.
