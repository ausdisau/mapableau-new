# Accessibility operations

Participants can submit accessibility support requests. Signals of type `accessibility` alert operators.

Critical journey and assistive technology test helpers live under `lib/pilot/accessibility/*`. Admin UI uses plain language and text status labels.
