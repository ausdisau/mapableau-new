# Operations command centre

`getPilotCommandCentre` returns pilot summary, operational snapshot, action queue, operators, and latest handover.

Admin UI: `/admin/pilot/[pilotId]/command`  
API: `GET /api/admin/pilots/[pilotId]/command`

Banner distinguishes sandbox vs limited_live with text labels (not colour-only).
