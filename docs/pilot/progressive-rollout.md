# Progressive rollout

Stage ladder moves one step at a time (retreat allowed for safety).

## Rules

- Advance requires `pilot:advance` and human rationale.
- Entering `limited_live` / `controlled_live` needs `limitedLiveEnabled` + assessment refs.
- **Limited live remains disabled by default.**
- **Pilot stage advance ≠ production approval.**
