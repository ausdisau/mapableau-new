# Corrective action

`PilotCorrectiveAction` types: process, training, configuration, communication, technical, other.

Statuses: open → in_progress → completed → verified (or cancelled).

API: `POST /api/admin/pilots/[pilotId]/corrective-actions`.
