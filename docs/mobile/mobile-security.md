# Mobile security

- Tokens only in SecureStore (keychain/keystore)
- No server secrets in the bundle
- Deep-link / universal-link / app-link allowlists
- Privacy-safe push previews
- Root/jailbreak signals are risk information only — never the sole bar to essential participant access without documented alternative
- Threat model: `apps/mobile/src/security/threat-model.md`
