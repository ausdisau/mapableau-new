# Incidents

Reuse canonical `IncidentReport` and `IndoorAccessibilityIncident`. AccessibilityOps must not create a second incident database.

Wave 1 links findings conceptually via shadow evaluation reason codes only. A future `AccessibilityIncidentExtension` may attach lifecycle fields (workaround, temporary access plan) without replacing Safety Centre ownership.

AI cannot close incidents.
