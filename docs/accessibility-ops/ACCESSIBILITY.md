# Accessibility of AccessibilityOps

Target WCAG 2.2 AA. Ops UI must provide:

- Keyboard completion and visible focus
- Semantic headings and accessible tables
- List alternatives for any graph or map
- No colour-only severity
- Zoom/reflow, reduced motion, plain language
- Easy incident-style feedback path that is itself accessible

Dogfood via Test Lab before supervised mode.
