# Prompt governance

Prompts are versioned software assets in `lib/ai/platform/prompts/registry.ts`.

Not editable through an ungoverned production UI. Do not expose prompt text publicly when it reveals security controls or private operational logic.
