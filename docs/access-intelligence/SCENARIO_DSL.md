# Scenario DSL / Flight simulator

Interview on Level 3 stages: orientation → prediction → investigation → decision → consequence (main-lift outage) → revision (western lift) → teach-back → reflection → transfer (19:00 Entrance B closed) → complete.

API: `/api/access-intelligence/scenarios/interview-level-3`

Uses Living Twin engines — not separate toy logic.
