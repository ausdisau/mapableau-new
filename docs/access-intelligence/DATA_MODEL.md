# Data model

Demo Living Twin fields: place, elements, features, evidence, nodes, edges, incidents, operatingRules, destinations, version.

Prisma `ai_*` tables exist for passports, places, features, evidence, routes, incidents, visit plans, audit. Default runtime: in-memory. Personal Access Twin = passport + JourneyContext (not persisted in MVP).
