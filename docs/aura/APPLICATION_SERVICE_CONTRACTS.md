# AURA Wave 3 — Application Service Contracts

Registry maps proposal types to future services (messaging, visit-plan share, notifications, transport, barrier reports). Wave 3 uses schema IDs, preflight validators, and metadata only — never write methods.
