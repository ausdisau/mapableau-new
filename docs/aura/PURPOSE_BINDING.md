# AURA Wave 3 — Purpose Binding

Purpose codes (`access.verify_venue_feature`, `access.share_visit_plan`, `support.notify_mission_change`, `transport.request_accessible_trip`, `access.report_barrier`) bind allowed action types, recipients, fields, expiry, and future consent. Vague/secondary-use purposes are rejected.
