# Threat Model (summary)

Priority threats: account takeover with custodial decrypt; capability theft/replay; supporter/recovery collusion; metadata leakage; XSS/localStorage mistakes; IDOR; import malware; offline approval replay; prompt injection; backup/log exposure.

Gates: step-up for export/recovery; lease tests; threshold recovery; privacy review; forbid plain localStorage; IDOR suite; quarantine; fresh compile; AI boundary tests; log redaction.
