# Deletion and Revocation

Vault deletion does not silently delete canonical records. Receipts distinguish system/local/key/recipient-attested/backup-pending/denied/limited. Recipient attestation is not independent proof. See `VAULT_DELETION_DISCLAIMER`.
