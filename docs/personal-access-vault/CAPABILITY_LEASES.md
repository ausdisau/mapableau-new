# Capability Leases

Vault capabilities are field/purpose/recipient/time-bound. Prefer extending RightsOS `RightsCapabilityLease` when merged; this branch stores shadow `VaultCapability` rows for simulation.

Broad OAuth tokens are not field policy. AURA mission leases remain separate operator authority.
