# Matter edge integration

**Status: SCAFFOLDED** (cluster descriptors + mapper). **Execute: NOT SUPPORTED.**

The web app is not a Matter controller and does not compile CHIP. Matter belongs on an edge / native host (or Home Assistant bridge) in a future phase.

P0 provides OnOff / LevelControl / DoorLock / WindowCovering / Thermostat fixtures and `MAPABLE_HOME_ENV_MATTER_ENABLED`.

Commissioning (`COMMISSION_DEVICE` / `SHARE_DEVICE`) is declared but **NOT SUPPORTED**.

**PROPOSED:** edge host + HA bridge. CHIP is reference material only.
