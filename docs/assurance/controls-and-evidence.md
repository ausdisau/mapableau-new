# Controls and evidence

Controls extend `SecurityControl`. Evidence lives in `AssuranceEvidence` with checksums, classification, and freshness windows. Legacy `SecurityEvidence` remains as a thin pointer.
