#!/usr/bin/env tsx
import { runAccessOpsScript } from "./_shared";

void runAccessOpsScript({
  name: "accessops-test-twin",
  category: "test",
  summary: "Test civic digital twin invariants and graph privacy boundaries.",
});
