#!/usr/bin/env tsx
import { runAccessOpsScript } from "./_shared";

void runAccessOpsScript({
  name: "accessops-test-routing",
  category: "test",
  summary: "Test advisory routing constraints, uncertainty, and continuity boundaries.",
});
