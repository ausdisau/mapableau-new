#!/usr/bin/env tsx
import { runAccessOpsScript } from "./_shared";

void runAccessOpsScript({
  name: "accessops-test-accessibility",
  category: "test",
  summary: "Test accessible operator shells and keyboard authoring paths.",
});
