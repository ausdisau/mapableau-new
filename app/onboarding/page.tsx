import { OnboardingBaseClient } from "./OnboardingBaseClient";

export const metadata = {
  title: "Complete registration | MapAble",
};

export default function OnboardingPage() {
  return <OnboardingBaseClient />;
}
