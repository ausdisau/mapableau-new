import { AdminDashboard } from "@/components/admin/back-of-house/AdminDashboard";

export const metadata = { title: "Operations command centre | MapAble Admin" };

export default function AdminOpsPage() {
  return <AdminDashboard />;
}
