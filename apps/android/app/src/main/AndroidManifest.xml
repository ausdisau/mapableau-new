<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".MapAbleApplication"
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.MapAble">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MapAble"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Verified App Links (Phase 06) — fallback to web if verification fails -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="mapable.com.au" android:pathPrefix="/access" />
                <data android:scheme="https" android:host="mapable.com.au" android:pathPrefix="/care" />
                <data android:scheme="https" android:host="mapable.com.au" android:pathPrefix="/transport" />
                <data android:scheme="https" android:host="mapable.com.au" android:pathPrefix="/jobs" />
                <data android:scheme="https" android:host="mapable.com.au" android:pathPrefix="/dashboard" />
                <data android:scheme="https" android:host="www.mapable.com.au" android:pathPrefix="/" />
            </intent-filter>
        </activity>
    </application>
</manifest>
