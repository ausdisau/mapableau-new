import { createPlaceholderAdapter } from "@/lib/verification/wwc/placeholder-wwc-adapter";

export const ntOchreCardAdapter = createPlaceholderAdapter(
  "Northern Territory Ochre Card (placeholder)",
  "NT",
  ["ochre_card"]
);
