# MapAble Foods

Routes: `/foods`, `/foods/menu`

API: `POST /api/foods/orders` (requires `allergyAcknowledged: true`)

Delivery is linked via transport orchestration adapters — not direct imports from the transport module.
